{
  "version": 2,
  "buildCommand": "cd ../.. && pnpm --filter @repo/db db:generate && pnpm exec turbo run build --filter=server",
  "builds": [
    {
      "src": "api/index.ts",
      "use": "@vercel/node"
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "api/index.ts"
    }
  ]
}
