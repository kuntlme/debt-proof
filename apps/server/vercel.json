{
  "version": 2,
  "framework": null,
  "buildCommand": "cd ../.. && pnpm --filter @repo/db db:generate && pnpm install && pnpm exec turbo run build --filter=server",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/api"
    }
  ]
}