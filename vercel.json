{
  "buildCommand": "pnpm --filter @repo/db db:generate && pnpm --filter @repo/db build && pnpm install && pnpm run build --filter=client",
  "outputDirectory": "apps/client/.next",
  "installCommand": "pnpm install",
  "framework": "nextjs"
}