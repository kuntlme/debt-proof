{
  "buildCommand": "pnpm --filter @repo/db db:generate && pnpm install && pnpm turbo run build --filter=client...",
  "outputDirectory": "apps/client/.next",
  "installCommand": "pnpm install",
  "framework": "nextjs"
}